# Rust Quick Start

## Cargo

**cargo new build** => builds a project directory, a Cargo.toml, a src directory, and a main.rs file. 

**cargo run** => compiles code and runs it.

## Rust Book

open an offline copy of the rust book with:

    ```
    rustup doc --book
    ```
